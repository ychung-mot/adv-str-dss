import { Routes } from '@angular/router';
import { ComponentsTestComponent } from './common/layout/components-test/components-test.component';

export const routes: Routes = [
    {
        path: '',
        component: ComponentsTestComponent
    },
];
